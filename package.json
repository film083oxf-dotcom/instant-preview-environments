{
  "name": "instant-preview-environments-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "preview": "wrangler preview",
    "deploy": "wrangler deploy",
    "typecheck": "node --check src/index.js && node --check src/auth.js && node --check src/preview-auth.js && node --check scripts/ai-diagnose.mjs && node --check scripts/generate-preview-config.mjs && node --check scripts/generate-production-config.mjs"
  },
  "devDependencies": {
    "wrangler": "4.136.3"
  }
}
